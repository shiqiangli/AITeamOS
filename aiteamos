#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${repo_root}/packages/schema:${repo_root}/packages/workspace:${repo_root}/packages/connectors:${repo_root}/services/api:${repo_root}/services/worker:${repo_root}:${PYTHONPATH:-}"
exec python3 -m tools.cli.aiteamos "$@"
