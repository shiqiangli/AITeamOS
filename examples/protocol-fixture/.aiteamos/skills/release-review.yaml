apiVersion: aiteamos.dev/v1alpha1
kind: Skill
metadata:
  name: release-review
spec:
  description: Review handoff packets, release notes, and memory proposal evidence.
  ownerMember: protocol-reviewer-human
  projects:
    - protocol-fixture
  capabilities:
    - assisted-ingest-review
    - human-approval
  requiredPermissions:
    - human-default
    - hybrid-default
  lifecycle: active
