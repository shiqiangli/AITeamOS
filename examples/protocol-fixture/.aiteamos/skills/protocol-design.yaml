apiVersion: aiteamos.dev/v1alpha1
kind: Skill
metadata:
  name: protocol-design
spec:
  description: Define and review protocol fixture operation contracts.
  ownerMember: protocol-architect
  projects:
    - protocol-fixture
  capabilities:
    - protocol-semantics
    - architecture-review
  requiredPermissions:
    - digital-default
  lifecycle: active
