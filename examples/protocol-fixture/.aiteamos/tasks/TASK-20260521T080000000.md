# Implement Add i32 runtime contract

Implement and verify `runtime.add_i32` runtime to `runtime.add_i32(lhs, rhs)`.
The run must produce a journal, review target, test evidence, and memory
proposal without approving its own memory.
