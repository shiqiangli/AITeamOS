# Approval workflows

This directory is the source-of-truth home for `ApprovalWorkflow` manifests. Workflows coordinate staged approval shape, SLA metadata, and escalation routing; they do not approve grants or execute work by themselves.
