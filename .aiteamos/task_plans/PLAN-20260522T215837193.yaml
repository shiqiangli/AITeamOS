apiVersion: aiteamos.dev/v1alpha1
kind: TaskPlan
metadata:
  id: PLAN-20260522T215837193
  createdAt: "2026-05-22T21:58:37.193+08:00"
spec:
  goal: Start multi-tenant namespace work before closing manager EvalSuite prerequisites.
  project: aiteamos
  createdByMember: aiteamos-manager
  status: rejected
  subtasks:
    - title: Add workspace namespace routing before the manager milestone is closed.
      assignedMember: backend-digital
      assignment: aiteamos-backend-runtime
      priority: normal
      acceptance:
        - Workspace namespace routing accepts multiple accounts.
      risks:
        - Skips the current lowest incomplete milestone.
      reviewGates:
        - Human rejects out-of-order milestone sequencing.
  risks:
    - Recommendation violates the current milestone ordering.
  reviewGates:
    - Human rejects the recommendation and records the reason.
