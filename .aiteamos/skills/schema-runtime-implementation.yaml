apiVersion: aiteamos.dev/v1alpha1
kind: Skill
metadata:
  name: schema-runtime-implementation
spec:
  description: Implement schema-backed workspace, API, and derived-index behavior.
  ownerMember: backend-digital
  projects:
  - aiteamos
  capabilities:
  - pydantic-schema
  - fastapi
  - sqlite-index
  requiredPermissions:
  - digital-default
  lifecycle: active
